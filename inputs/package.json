{
  "name": "@ez-elements/inputs",
  "version": "0.1.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc --build ./tsconfig.json",
    "watch": "npm run build -- --watch",
    "test": "../node_modules/.bin/jest ./src --coverage",
    "test:watch": "npm run test -- --watch",
    "coveralls": "../node_modules/.bin/jest ./src --coverage --coverageReporters=text-lcov | ../node_modules/.bin/coveralls"
  },
  "author": "Marcus Longmuir",
  "license": "Apache-2.0",
  "peerDependencies": {
    "@ez-elements/core": "*"
  },
  "devDependencies": {
    "@ez-elements/core": "*"
  }
}
